-- No data-quality rules configured — all rows pass through.
-- Add and approve rules in Discovery to enable pre-processing filters.
SELECT NULL AS "RuleId", NULL AS "__table", NULL AS "__row" WHERE 1 = 0
